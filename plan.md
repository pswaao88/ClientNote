# plan.md — 거래처/복사기 관리 앱 (아빠 전용, Android Only)

## 0. 목적 / 전제
- **목적**: 아빠가 거래처별로 복사기(모델/시리얼)와 **소모품 교체 내역**, **수리 내역**을 빠르게 기록/조회
- **배포**: 스토어 등록 없음, **APK로 설치**(가족/본인 기기만)
- **계정/서버**: 로그인/서버 없이 **로컬 저장 우선**
- **핵심 UX**: 현장에서 3~4번 터치로 “수리 기록 추가”까지 도달
- **개발 우선순위**: 기능 완성보다 **UI/UX 동선 검증 먼저** (더미 데이터로 화면부터 확인)

---

## 1. 개발 스택 정리

### 1) 언어/플랫폼
- Kotlin
- Android (minSdk 26 권장)

### 2) UI
- Jetpack Compose
- Material 3

### 3) 아키텍처
- MVVM (ViewModel + StateFlow)
- Repository 패턴
- (선택) UseCase 레이어: 규모 커지면 분리

### 4) 데이터 저장
- Room (SQLite)
- Kotlinx Serialization (선택: 백업/복원 JSON)

### 5) 비동기/상태
- Kotlin Coroutines
- Flow / StateFlow

### 6) 지도(거래처 위치 선택)
- 1안: Google Maps SDK (API 키 필요)
- 2안: OSM 기반 라이브러리(OSMDroid 등) (API 키 없이 가능, 설정은 약간 더 필요)
- MVP에서는 **지도 선택 기능을 Step 후반에 붙이는 전략** 권장

### 7) 사진 첨부(옵션)
- Android Photo Picker
- URI 저장(파일 복사 없이 URI만 저장) 또는 앱 전용 저장소로 복사

---

## 2. 데이터 모델 (Room Entity 초안)

### 관계
- Customer(거래처) 1 ─ N Copier(복사기)
- Copier 1 ─ N ConsumableLog(교체 내역)
- Copier 1 ─ N RepairLog(수리 내역)

### Customer
- id (PK)
- name (거래처명)
- lat, lng (선택)
- address (선택)
- phone (선택)
- memo (선택)
- createdAt, updatedAt

### Copier
- id (PK)
- customerId (FK)
- brand (제조사)
- model (모델명)
- serial (선택)
- installedAt (선택)
- memo (선택)
- createdAt, updatedAt

### ConsumableLog (소모품 교체)
- id (PK)
- copierId (FK)
- partType (예: TONER/DRUM/FUSER/ETC)
- partName (자유 입력, 선택)
- replacedAt (일시)
- qty (선택)
- cost (선택)
- note (선택)

### RepairLog (수리)
- id (PK)
- copierId (FK)
- occurredAt (일시)
- symptom (증상)
- actionTaken (조치)
- cost (선택)
- nextAction (선택)
- photoUri (선택)

---

## 3. MVP 기능 구체화 (필수 / 선택)

### 3.1 필수(MVP)
1) 거래처 관리
- 거래처 추가/수정/삭제
- 거래처 목록(검색)
- 거래처 상세: 기본 정보 + 등록된 복사기 리스트

2) 복사기 관리(거래처 하위)
- 복사기 추가/수정/삭제
- 복사기 상세: 모델/시리얼/메모 + 이력 탭(교체/수리)

3) 이력 관리
- 소모품 교체 내역 CRUD
- 수리 내역 CRUD
- 최신순 정렬

4) 검색/정렬
- 거래처명 검색
- 복사기 모델/시리얼 검색(선택: 전체 검색 화면)

### 3.2 선택(가능하면)
- 모델 입력 자동완성(최근 입력 기반)
- “자주 쓰는 모델” 즐겨찾기 템플릿
- 수리 사진 첨부
- 백업/복원(export/import): JSON 또는 DB 파일

### 3.3 나중(확장)
- 방문 일정/체크리스트
- 알림(소모품 교체 주기)
- 외부 공유(엑셀/CSV export)
- 다나와급 모델 DB(현실적으로 유지비 큼 → 후순위)

---

## 4. 개발 Step (UI/UX 선행 방식으로 재정렬)

> 원칙: **먼저 화면과 동선을 보여주고**, 데이터/저장은 뒤에서 연결한다.
> 즉, 더미 데이터 기반으로 “사용 흐름”부터 검증 후 Room CRUD를 붙인다.

### Step 0 — 프로젝트 세팅
- [x] Android Studio 프로젝트 생성 (Compose + Material3)
- [x] 패키지 구조 생성
- [x] 기본 Navigation 구성 (Customers → CustomerDetail → CopierDetail)
- 산출물: 앱 실행 + 빈 화면 네비게이션 동작 (문서 기준 완료)

### Step 1 — UI/UX 골격 먼저 만들기 (더미 데이터)
- [x] CustomerListScreen: 검색창 + FAB + 리스트 카드 UI
- [x] CustomerDetailScreen: 거래처 정보 카드 + 복사기 리스트 UI
- [x] CopierDetailScreen: 상단 정보 + 탭(Consumables / Repairs) UI
- [x] Add/Edit 화면들 폼 배치(저장 버튼은 임시 동작)
- 산출물: **아빠에게 먼저 보여줄 수 있는 클릭 가능한 프로토타입** (문서 시안 완료)

### Step 2 — UX 다듬기(현장 사용성 우선)
- [ ] 수리기록 추가까지 3~4탭 이내 동선 점검
- [ ] 버튼 위치/크기/텍스트 단순화(큰 버튼, 쉬운 문구)
- [ ] 빈 상태(데이터 없음) 안내 문구 적용
- [ ] 삭제/수정 확인 다이얼로그 UX 통일
- 산출물: **실사용에 가까운 UI/UX 확정안**

### Step 3 — DB/도메인 연결 (Room)
- [ ] Entity 정의(Customer/Copier/Logs)
- [ ] DAO 작성(CRUD, 관계 조회)
- [ ] AppDatabase 구성
- [ ] Repository 작성
- 산출물: 더미 데이터 화면에 실제 데이터 연결

### Step 4 — 거래처/복사기 CRUD 연결
- [ ] 거래처 추가/수정/삭제 실제 동작
- [ ] 복사기 추가/수정/삭제 실제 동작
- [ ] 검색 연동(거래처명)
- 산출물: 거래처 → 복사기 흐름 완성

### Step 5 — 교체/수리 내역 CRUD 연결 (핵심 MVP)
- [ ] 교체 내역 탭: 목록/추가/수정/삭제
- [ ] 수리 내역 탭: 목록/추가/수정/삭제
- [ ] 최신순 정렬
- 산출물: 현장 기록 핵심 동선 완성

### Step 6 — 검색/정렬 강화 + 입력 UX
- [ ] 거래처/복사기 통합 검색(선택)
- [ ] 모델 입력 자동완성(최근 입력 기반: ModelHistory 테이블 or distinct 쿼리)
- 산출물: “빠르게 찾고 기록” UX 개선

### Step 7 — 지도 위치 선택(MapPicker)
- [ ] MapPicker 화면 추가
- [ ] 거래처 추가/수정에서 “지도에서 선택” 버튼
- [ ] 좌표 선택 → 역지오코딩으로 주소 표시(선택)
- 산출물: 거래처 위치까지 포함한 실사용 형태

### Step 8 — 백업/복원(강추)
- [ ] JSON export/import (가장 안전/유지 쉬움)
  - export: Customer + Copier + Logs 전체를 JSON으로
  - import: 충돌 정책(덮어쓰기/추가) 결정
- 또는 DB 파일 백업(권한/경로 이슈 고려)
- 산출물: 폰 교체/초기화 대비 가능

### Step 9 — 품질/배포
- [ ] 입력 검증(거래처명 필수, 모델명 필수 등)
- [ ] 에러 처리
- [ ] 릴리즈 빌드 + APK 생성
- [ ] 간단 사용 매뉴얼(아빠용 1페이지)
- 산출물: 설치 가능한 APK + 사용 문서

---

## 5. 화면 목록 (최소 구성)
- CustomerListScreen
- CustomerEditScreen
- CustomerDetailScreen
- CopierEditScreen
- CopierDetailScreen (Tabs: Consumables / Repairs)
- ConsumableLogEditScreen
- RepairLogEditScreen
- (Optional) MapPickerScreen
- (Optional) GlobalSearchScreen
- (Optional) BackupRestoreScreen

---

## 6. Done 정의(완료 기준)
MVP 완료 기준:
- 거래처 추가 → 복사기 추가 → 수리/교체 기록 추가가 막힘 없이 가능
- 앱 재실행 후 데이터 유지(Room)
- 거래처 검색으로 빠르게 진입 가능
- 삭제/수정 정상 동작
- **아빠가 UI를 보고 1분 내 주요 동선(조회/기록)을 이해 가능**

---

## 7. 리스크 / 결정 사항
- 지도: Google Maps SDK 사용 시 API 키 관리 필요(개인 앱이라도 과금 정책 확인)
- 사진: URI만 저장하면 권한/수명 이슈 가능 → 필요하면 앱 저장소로 복사
- 백업: DB 파일보다 JSON이 이식성/안정성 좋음(추천)

---

## 8. UI 프로토타입 시안 (2안)

> 목적: 실제 코딩 전에 아빠가 보기 쉬운 동선을 먼저 확정하기 위한 화면 시안.

### 프로토타입 A — 리스트 중심(정보 밀도형)
- 특징
  - 한 화면에 거래처/기기 정보를 많이 보여줘 탐색 속도 우선
  - 익숙한 리스트+카드 형태
  - 검색 중심 업무에 유리

#### A-1) CustomerListScreen
```text
┌──────────────────────────────┐
│ 거래처 목록                  │
│ [🔍 거래처명 검색__________] │
├──────────────────────────────┤
│ [신광사무기]                 │
│  - 기기 3대 / 최근수리 2일전 │
├──────────────────────────────┤
│ [한빛인쇄]                   │
│  - 기기 1대 / 최근교체 5일전 │
├──────────────────────────────┤
│ [대성복사]                   │
│  - 기기 2대 / 최근수리 오늘  │
└──────────────────────────────┘
                      [ + ] FAB
```

#### A-2) CopierDetailScreen
```text
┌──────────────────────────────┐
│ Xerox C226 (S/N 23A-11)      │
│ 고객: 신광사무기             │
│ [수리추가] [교체추가]        │
├──────────────────────────────┤
│ [탭] 교체내역 | 수리내역      │
├──────────────────────────────┤
│ 2026-02-18  토너 교체        │
│ 2026-02-10  드럼 교체        │
│ ...                          │
└──────────────────────────────┘
```

- 장점: 데이터 확인이 빠르고 관리 화면에 익숙한 사용자에게 효율적
- 단점: 고령 사용자에게 정보량이 많아 복잡해 보일 수 있음

---

### 프로토타입 B — 작업 중심(큰 버튼형)
- 특징
  - “무엇을 할지”를 먼저 고르게 하는 작업형 구조
  - 버튼 크기/터치 영역 확대
  - 현장 입력 속도와 실수 방지 우선

#### B-1) Home Quick Action
```text
┌──────────────────────────────┐
│ 오늘 작업                    │
│ [거래처 찾기]                │
│ [수리 기록 바로 추가]        │
│ [소모품 교체 바로 추가]      │
│ [최근 작업 보기]             │
└──────────────────────────────┘
```

#### B-2) Repair Quick Add Flow
```text
Step1) 거래처 선택  ->  Step2) 기기 선택  ->  Step3) 증상/조치 입력

┌──────────────────────────────┐
│ 수리 기록 추가 (3/3)         │
│ 증상: [___________________]  │
│ 조치: [___________________]  │
│ 비용: [________]             │
│ [저장] (큰 버튼)             │
└──────────────────────────────┘
```

- 장점: 신규 사용자/고령 사용자에게 직관적, “3~4터치 목표” 달성 쉬움
- 단점: 상세 데이터 탐색은 A안보다 느릴 수 있음

---

### 추천
- 초기 사용자(아빠) 기준으로는 **B안(작업 중심)**을 기본으로 추천.
- 단, 관리 효율을 위해 리스트 확인이 필요한 화면(예: 거래처 목록)은 A안 요소를 혼합한 **하이브리드**가 현실적.
- 즉, 메인 진입은 B안(큰 버튼), 상세 조회는 A안(리스트 밀도)로 구성.

---

## 9. 참고 이미지 기반 UI 반응형 적용 계획 (Android)

> 제공해주신 시안(상단 AppBar + 검색/폼 + 하단 주요 CTA + 카드 리스트)을 기준으로,
> Android 화면 크기별로 레이아웃이 자연스럽게 변하도록 구현 계획을 명시한다.

### 9.1 기본 디자인 원칙 (이미지 반영)
- 공통
  - 상단: 파란색 계열 TopAppBar + 명확한 화면 타이틀
  - 본문: 카드/입력 필드 중심의 단순한 세로 흐름
  - 하단: 저장/추가 같은 핵심 CTA는 화면 하단 고정 또는 매우 눈에 띄게 배치
- 조작 원칙
  - 자주 쓰는 동작(수리 추가/교체 추가)은 1차 버튼으로 강조
  - 텍스트는 짧고 직관적으로 유지 (예: “저장”, “수리 추가”)
  - 아이콘은 보조수단으로만 사용, 텍스트 라벨 필수

### 9.2 반응형 기준 (Compose Window Size Class)
- Compact (대부분 일반 스마트폰 세로)
  - 단일 컬럼, 세로 스크롤 중심
  - 하단 고정 CTA 1개(필요 시 FAB 보조)
- Medium (큰 폰 가로 / 작은 태블릿)
  - 2영역 분할(예: 좌측 리스트 + 우측 상세 미리보기)
  - 폼 화면은 입력영역/보조영역(사진, 최근 이력) 분리
- Expanded (태블릿)
  - 마스터-디테일 고정(거래처/기기 리스트 + 상세 패널 동시 표시)
  - 탭/이력 목록은 한 화면에서 더 많은 정보 표시

### 9.3 화면별 반응형 동작 규칙
1) CustomerListScreen
- Compact: 검색창 + 리스트 + 하단 거래처 추가 버튼/FAB
- Medium+: 검색/필터는 상단, 좌측 거래처 리스트/우측 최근 작업 카드 동시 표시

2) CustomerEditScreen
- Compact: 입력 필드 전부 단일 세로 배치
- Medium+: 기본정보(이름/연락처)와 부가정보(메모/지도 선택) 2컬럼

3) CustomerDetailScreen
- Compact: 기본정보 → 지도 미리보기 → 복사기 목록 순서
- Medium+: 좌측 기본정보+지도, 우측 복사기 목록

4) CopierDetailScreen
- Compact: 기기 정보 카드 → 교체/수리 탭 → 하단 “내역 추가” 버튼
- Medium+: 좌측 기기 정보/사진, 우측 탭 이력

5) RepairLogEditScreen
- Compact: 날짜/증상/조치/비용 + 하단 고정 저장 버튼
- Medium+: 입력 폼(좌) + 첨부/미리보기(우) 분할

### 9.4 접근성/가독성 기준 (아빠 사용성 중심)
- 최소 터치 영역 48dp 이상
- 본문 텍스트 기본 16sp, 주요 버튼은 더 크게
- 명도 대비 충분히 확보(파란 헤더 + 흰 배경 + 진한 텍스트)
- 키보드 열림 시 저장 버튼이 가려지지 않도록 insets 처리
- 오류 메시지는 기술 용어 대신 쉬운 문장 사용

### 9.5 구현 체크리스트 (문서 기준)
- [ ] WindowSizeClass 기준으로 레이아웃 분기 설계
- [ ] 모든 주요 화면에서 Compact/Medium/Expanded 와이어프레임 확정
- [ ] 세로/가로 전환 시 상태 유지(입력값, 탭 위치)
- [ ] 하단 CTA 겹침/가림(insets) 점검
- [ ] 실제 기기 2종 이상에서 터치 동선(3~4탭 목표) 확인

### 9.6 Step 반영 업데이트
- 기존 Step 1~2(UI/UX 선행)에 아래 항목 추가:
  - Step 1: 각 화면 Compact 버전 우선 구현
  - Step 2: Medium/Expanded 반응형 분기 및 가로모드 검증
- 목적: “먼저 보여주는 프로토타입”이 실제 안드로이드 기기에서도 깨지지 않게 보장


## 10. Step 1 진행 결과 (문서 프로토타입 완료)

> 요청사항(“Step1까지 진행”)에 맞춰, 런타임 코드 변경 없이 **화면 구조/동선/반응형 기준**을 확정했다.

### 10.1 확정된 화면 프로토타입 범위
- CustomerListScreen
  - 상단 검색바 + 거래처 카드 리스트 + 우하단 FAB
- CustomerDetailScreen
  - 거래처 기본정보/연락처 + 지도 미리보기 + 복사기 목록
- CopierDetailScreen
  - 기기 정보 카드 + 교체/수리 탭 + 하단 내역 추가 CTA
- Add/Edit 폼 화면
  - 거래처/복사기/수리 입력 폼, 저장 버튼은 임시 동작 기준

### 10.2 Step1 동선(클릭 가능한 흐름)
- CustomerList → CustomerDetail → CopierDetail
- CustomerList → CustomerEdit(추가)
- CustomerDetail → CopierEdit(추가)
- CopierDetail → RepairLogEdit / ConsumableLogEdit(추가)

### 10.3 Step1 더미 데이터 기준
- 거래처 3개(주소/연락처 포함)
- 거래처별 복사기 1~3대
- 복사기별 교체/수리 내역 각 2건 이상
- 빈 상태 화면 1개(데이터 없는 거래처)

### 10.4 Step1 반응형 적용 범위
- Compact: 단일 컬럼 + 하단 주요 버튼
- Medium/Expanded: 리스트-상세 2영역 분할 시안
- 세로/가로 전환 시 입력값 보존을 Step2에서 검증하도록 사전 정의

### 10.5 Step1 완료 기준 체크
- [x] 핵심 3개 조회 화면 구조 정의 완료
- [x] Add/Edit 입력 폼 구조 정의 완료
- [x] 주요 이동 경로(수리 기록 추가까지) 정의 완료
- [x] 반응형 기준(Compact/Medium/Expanded) 연결 완료

### 10.6 Step2로 넘길 항목
- 버튼 라벨 단순화(A/B 문구 비교)
- 고령 사용자 가독성 검증(글자/간격/대비)
- 실제 기기 터치 동선 테스트(3~4탭 목표)


## 11. Step 0 진행 결과 (문서 기준 완료)

> 요청사항(“Step0까지 적용”)에 맞춰, 코드 구현이 아닌 **설계/준비 관점**에서 Step0 완료 상태를 문서에 반영했다.

### 11.1 Step0 완료 체크
- [x] Android Studio 프로젝트 생성 기준 확정 (Compose + Material3)
- [x] 패키지 구조 방향 확정
- [x] 기본 네비게이션 흐름 정의 (Customers → CustomerDetail → CopierDetail)

### 11.2 Step0 산출물 정의
- 앱 실행 + 빈 화면 네비게이션 동작을 초기 완료 기준으로 설정
- Step1 UI 프로토타입과 연결되도록 화면 진입 경로 선확정

### 11.3 다음 단계 연결
- Step1에서 더미 데이터 기반 화면 골격 구현으로 즉시 전환
- Step2에서 사용성/반응형 검증 강화
